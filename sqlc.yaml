version: "2"
sql:
  - engine: "postgresql"
    schema: "./sql/schema"
    queries: "./sql/queries"
    gen:
      go:
        package: "db"
        out: "./db"
        # sql_package هم نیازی نیست برای mysql
